#include <tunables/global>

profile coturn flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Capabilities
  capability,
  file,
  signal (send) set=(kill,term,int,hup,cont),

  # S6-Overlay
  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,
  /dev/tty rw,

  # Bashio
  /usr/lib/bashio/** ix,
  /tmp/** rwk,

  # Access to options.json and other add-on files
  /data/** rw,

  # Start new shells
  /bin/bash ix,
  /usr/bin/env ix,
  /bin/echo ix,
  /bin/cat ix,
  /bin/ls ix,

  # Access to SSL certificates
  /ssl/** r,

  # Coturn specific
  /usr/bin/turnserver ix,
  /etc/coturn/** rw,
  /var/lib/coturn/** rw,
  /var/log/coturn/** rw,
  /var/run/coturn/** rw,

  # System files
  /etc/passwd r,
  /etc/group r,
  /etc/hosts r,
  /etc/resolv.conf r,
  /etc/nsswitch.conf r,
  /etc/ssl/certs/** r,
  /etc/ca-certificates/** r,
  /usr/share/ca-certificates/** r,

  # Network access
  network,

  # Process management
  /proc/*/stat r,
  /proc/*/status r,
  /proc/sys/kernel/pid_max r,
  @{PROC}/sys/net/core/rmem_default r,
  @{PROC}/sys/net/core/rmem_max r,
  @{PROC}/sys/net/core/wmem_default r,
  @{PROC}/sys/net/core/wmem_max r,

  # Device access
  /dev/null rw,
  /dev/zero r,
  /dev/random r,
  /dev/urandom r,

  # Deny some dangerous paths
  deny /proc/kcore r,
  deny /proc/sysrq-trigger w,
  deny /sys/kernel/security/** rw,
}
